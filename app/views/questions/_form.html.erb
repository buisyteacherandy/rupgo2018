<%= simple_form_for([@deal,@question]) do |f| %>
  <%= f.error_notification %>

  <div class="centerforms">
    <%= f.input :body %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, (t ".question"), :class => "btn btn-success"%>
  </div>
<% end %>
