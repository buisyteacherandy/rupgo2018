<%= simple_form_for([@destination,@comment]) do |f| %>
  <%= f.error_notification %>

  <div class="centerforms">
    <%= f.input :body %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, (t ".comment"), :class => "btn btn-success"%>

  </div>
<% end %>
