

<div class="img-deals">
  
  <h3><%= t ".outlet" %></h3>

<p id="notice alert"><%= notice %></p>
</div>



<div class="container-fluid options2">
    <nav class="breadcrumb">
      <li><%= link_to "Home", destinations_path, :class => "breadcrumb-item" %> </li>
      <li><span class="breadcrumb-item active"><%= t ".hotdeals" %></span></li>
    </nav>
  <div class="row main-rows">

 <%= render @deals %> 
 </div>  
</div> <!-- container -->
<!-- <script>
$('.owl-carousel').owlCarousel({
    loop:true,
    margin:10,
    nav:true,
    
    responsive:{
        0:{
            items:1
        },
        600:{
            items:1
        },
        1000:{
            items:1
        }
    }
});
      // For Demo purposes only (show hover effect on mobile devices)
      [].slice.call( document.querySelectorAll('a[href="#"') ).forEach( function(el) {
        el.addEventListener( 'click', function(ev) { ev.preventDefault(); } );
      } );


</script> -->

<hr class="style-two">


</div>